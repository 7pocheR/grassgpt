=== TIER 1 EXPERIMENT 1 (CONTINUATION): 36L, r=384, Hybrid ===
Job ID: 610890
Node: m001
Start time: Fri Jan  2 11:39:16 AM CST 2026

Resuming from checkpoint: /net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384/ckpt.pt

Overriding config with config/train_gpt2_large_grassmann_hybridgate_r384.py:
# TIER 1 EXPERIMENT 1: 36L, r=384, hybrid gating
# Question: Is 36L slow because of depth or low rank?
# Hypothesis: If r=384 converges faster than r=256, rank was the bottleneck

# GPT-2 Large-ish architecture
n_layer = 36  # Deep network
n_head = 16
n_embd = 1024
block_size = 1024
dropout = 0.1  # Standard (unified across all Tier 1 experiments)
bias = False  # Grassmann requires no bias

# Enable Grassmann with hybrid gating
use_grassmann = True
use_full_block_decomp = False  # Hybrid gating (not full block decomp)

# Grassmann configuration
# INCREASED rank from 256 → 384 (37.5% of n_embd, matching 24L experiments)
grass_rank = 384  # CRITICAL: Higher rank to test expressivity hypothesis
grass_scale = 10.0  # Uniform x=10 scaling
grass_a = 1.0  # Identity-centered for non-skip layers
grass_b = 0.0
grass_lr = 2e-3  # Unified across all Tier 1 experiments

# Hybrid gating architecture:
# - c_attn: 48 head-level gates (16 heads × 3 QKV), linear + sigmoid
# - mlp.c_fc: 4 block-level gates (4 MLP blocks), linear + sigmoid
gate_lr = 1.2e-3  # 2× learning_rate (unified)
embed_lr = 3e-4   # 0.5× learning_rate for embeddings

# Training hyperparameters - TARGET: 100 tokens/param
# Total params: ~660M → 66B tokens → 168k iters @ 393K tokens/iter
learning_rate = 6e-4  # Base LR for AdamW components
max_iters = 168000  # 100 tokens/param
warmup_iters = 2000  # Standard warmup (unified)
lr_decay_iters = 150000  # CRITICAL: < max_iters for fine-tuning region!
min_lr = 6e-5  # 0.1 × learning_rate

# Multi-GPU batch configuration
# Same as before: 32 × 12 × 4 GPUs = 1536 sequences × 1024 seq_len = 1,572,864 tokens
# grad_accum MUST be divisible by 4 GPUs (DDP requirement)
batch_size = 32  # Per GPU
gradient_accumulation_steps = 12  # Total 384 sequences per GPU, 393K tokens/iter

# Regularization
weight_decay = 1e-1  # Standard for GPT-2 (applies to AdamW params only)

# Optimizer
beta1 = 0.9
beta2 = 0.95
grad_clip = 1.0

# Evaluation
eval_interval = 1000
eval_iters = 200
log_interval = 10

# Dataset
dataset = 'openwebtext'
data_dir = '/net/scratch2/junyuren/nanoGPT-manifold/data/openwebtext'
always_save_checkpoint = True

# CRITICAL: Save to scratch (home has strict disk quota!)
out_dir = '/net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384'

# Compile for performance
compile = True

# System
dtype = 'bfloat16'  # More stable than float16 for Grassmann
device = 'cuda'

# Phase identifier (for configure_optimizers_grassmann_phases)
grassmann_phase = 'phase3'  # c_attn + mlp.c_fc (non-skip-facing only)

Overriding: init_from = resume
Overriding config with config/train_gpt2_large_grassmann_hybridgate_r384.py:
# TIER 1 EXPERIMENT 1: 36L, r=384, hybrid gating
# Question: Is 36L slow because of depth or low rank?
# Hypothesis: If r=384 converges faster than r=256, rank was the bottleneck

# GPT-2 Large-ish architecture
n_layer = 36  # Deep network
n_head = 16
n_embd = 1024
block_size = 1024
dropout = 0.1  # Standard (unified across all Tier 1 experiments)
bias = False  # Grassmann requires no bias

# Enable Grassmann with hybrid gating
use_grassmann = True
use_full_block_decomp = False  # Hybrid gating (not full block decomp)

# Grassmann configuration
# INCREASED rank from 256 → 384 (37.5% of n_embd, matching 24L experiments)
grass_rank = 384  # CRITICAL: Higher rank to test expressivity hypothesis
grass_scale = 10.0  # Uniform x=10 scaling
grass_a = 1.0  # Identity-centered for non-skip layers
grass_b = 0.0
grass_lr = 2e-3  # Unified across all Tier 1 experiments

# Hybrid gating architecture:
# - c_attn: 48 head-level gates (16 heads × 3 QKV), linear + sigmoid
# - mlp.c_fc: 4 block-level gates (4 MLP blocks), linear + sigmoid
gate_lr = 1.2e-3  # 2× learning_rate (unified)
embed_lr = 3e-4   # 0.5× learning_rate for embeddings

# Training hyperparameters - TARGET: 100 tokens/param
# Total params: ~660M → 66B tokens → 168k iters @ 393K tokens/iter
learning_rate = 6e-4  # Base LR for AdamW components
max_iters = 168000  # 100 tokens/param
warmup_iters = 2000  # Standard warmup (unified)
lr_decay_iters = 150000  # CRITICAL: < max_iters for fine-tuning region!
min_lr = 6e-5  # 0.1 × learning_rate

# Multi-GPU batch configuration
# Same as before: 32 × 12 × 4 GPUs = 1536 sequences × 1024 seq_len = 1,572,864 tokens
# grad_accum MUST be divisible by 4 GPUs (DDP requirement)
batch_size = 32  # Per GPU
gradient_accumulation_steps = 12  # Total 384 sequences per GPU, 393K tokens/iter

# Regularization
weight_decay = 1e-1  # Standard for GPT-2 (applies to AdamW params only)

# Optimizer
beta1 = 0.9
beta2 = 0.95
grad_clip = 1.0

# Evaluation
eval_interval = 1000
eval_iters = 200
log_interval = 10

# Dataset
dataset = 'openwebtext'
data_dir = '/net/scratch2/junyuren/nanoGPT-manifold/data/openwebtext'
always_save_checkpoint = True

# CRITICAL: Save to scratch (home has strict disk quota!)
out_dir = '/net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384'

# Compile for performance
compile = True

# System
dtype = 'bfloat16'  # More stable than float16 for Grassmann
device = 'cuda'

# Phase identifier (for configure_optimizers_grassmann_phases)
grassmann_phase = 'phase3'  # c_attn + mlp.c_fc (non-skip-facing only)

Overriding: init_from = resume
Overriding config with config/train_gpt2_large_grassmann_hybridgate_r384.py:
# TIER 1 EXPERIMENT 1: 36L, r=384, hybrid gating
# Question: Is 36L slow because of depth or low rank?
# Hypothesis: If r=384 converges faster than r=256, rank was the bottleneck

# GPT-2 Large-ish architecture
n_layer = 36  # Deep network
n_head = 16
n_embd = 1024
block_size = 1024
dropout = 0.1  # Standard (unified across all Tier 1 experiments)
bias = False  # Grassmann requires no bias

# Enable Grassmann with hybrid gating
use_grassmann = True
use_full_block_decomp = False  # Hybrid gating (not full block decomp)

# Grassmann configuration
# INCREASED rank from 256 → 384 (37.5% of n_embd, matching 24L experiments)
grass_rank = 384  # CRITICAL: Higher rank to test expressivity hypothesis
grass_scale = 10.0  # Uniform x=10 scaling
grass_a = 1.0  # Identity-centered for non-skip layers
grass_b = 0.0
grass_lr = 2e-3  # Unified across all Tier 1 experiments

# Hybrid gating architecture:
# - c_attn: 48 head-level gates (16 heads × 3 QKV), linear + sigmoid
# - mlp.c_fc: 4 block-level gates (4 MLP blocks), linear + sigmoid
gate_lr = 1.2e-3  # 2× learning_rate (unified)
embed_lr = 3e-4   # 0.5× learning_rate for embeddings

# Training hyperparameters - TARGET: 100 tokens/param
# Total params: ~660M → 66B tokens → 168k iters @ 393K tokens/iter
learning_rate = 6e-4  # Base LR for AdamW components
max_iters = 168000  # 100 tokens/param
warmup_iters = 2000  # Standard warmup (unified)
lr_decay_iters = 150000  # CRITICAL: < max_iters for fine-tuning region!
min_lr = 6e-5  # 0.1 × learning_rate

# Multi-GPU batch configuration
# Same as before: 32 × 12 × 4 GPUs = 1536 sequences × 1024 seq_len = 1,572,864 tokens
# grad_accum MUST be divisible by 4 GPUs (DDP requirement)
batch_size = 32  # Per GPU
gradient_accumulation_steps = 12  # Total 384 sequences per GPU, 393K tokens/iter

# Regularization
weight_decay = 1e-1  # Standard for GPT-2 (applies to AdamW params only)

# Optimizer
beta1 = 0.9
beta2 = 0.95
grad_clip = 1.0

# Evaluation
eval_interval = 1000
eval_iters = 200
log_interval = 10

# Dataset
dataset = 'openwebtext'
data_dir = '/net/scratch2/junyuren/nanoGPT-manifold/data/openwebtext'
always_save_checkpoint = True

# CRITICAL: Save to scratch (home has strict disk quota!)
out_dir = '/net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384'

# Compile for performance
compile = True

# System
dtype = 'bfloat16'  # More stable than float16 for Grassmann
device = 'cuda'

# Phase identifier (for configure_optimizers_grassmann_phases)
grassmann_phase = 'phase3'  # c_attn + mlp.c_fc (non-skip-facing only)

Overriding: init_from = resume
tokens per iteration will be: 393,216
Resuming training from /net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384
Overriding config with config/train_gpt2_large_grassmann_hybridgate_r384.py:
# TIER 1 EXPERIMENT 1: 36L, r=384, hybrid gating
# Question: Is 36L slow because of depth or low rank?
# Hypothesis: If r=384 converges faster than r=256, rank was the bottleneck

# GPT-2 Large-ish architecture
n_layer = 36  # Deep network
n_head = 16
n_embd = 1024
block_size = 1024
dropout = 0.1  # Standard (unified across all Tier 1 experiments)
bias = False  # Grassmann requires no bias

# Enable Grassmann with hybrid gating
use_grassmann = True
use_full_block_decomp = False  # Hybrid gating (not full block decomp)

# Grassmann configuration
# INCREASED rank from 256 → 384 (37.5% of n_embd, matching 24L experiments)
grass_rank = 384  # CRITICAL: Higher rank to test expressivity hypothesis
grass_scale = 10.0  # Uniform x=10 scaling
grass_a = 1.0  # Identity-centered for non-skip layers
grass_b = 0.0
grass_lr = 2e-3  # Unified across all Tier 1 experiments

# Hybrid gating architecture:
# - c_attn: 48 head-level gates (16 heads × 3 QKV), linear + sigmoid
# - mlp.c_fc: 4 block-level gates (4 MLP blocks), linear + sigmoid
gate_lr = 1.2e-3  # 2× learning_rate (unified)
embed_lr = 3e-4   # 0.5× learning_rate for embeddings

# Training hyperparameters - TARGET: 100 tokens/param
# Total params: ~660M → 66B tokens → 168k iters @ 393K tokens/iter
learning_rate = 6e-4  # Base LR for AdamW components
max_iters = 168000  # 100 tokens/param
warmup_iters = 2000  # Standard warmup (unified)
lr_decay_iters = 150000  # CRITICAL: < max_iters for fine-tuning region!
min_lr = 6e-5  # 0.1 × learning_rate

# Multi-GPU batch configuration
# Same as before: 32 × 12 × 4 GPUs = 1536 sequences × 1024 seq_len = 1,572,864 tokens
# grad_accum MUST be divisible by 4 GPUs (DDP requirement)
batch_size = 32  # Per GPU
gradient_accumulation_steps = 12  # Total 384 sequences per GPU, 393K tokens/iter

# Regularization
weight_decay = 1e-1  # Standard for GPT-2 (applies to AdamW params only)

# Optimizer
beta1 = 0.9
beta2 = 0.95
grad_clip = 1.0

# Evaluation
eval_interval = 1000
eval_iters = 200
log_interval = 10

# Dataset
dataset = 'openwebtext'
data_dir = '/net/scratch2/junyuren/nanoGPT-manifold/data/openwebtext'
always_save_checkpoint = True

# CRITICAL: Save to scratch (home has strict disk quota!)
out_dir = '/net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384'

# Compile for performance
compile = True

# System
dtype = 'bfloat16'  # More stable than float16 for Grassmann
device = 'cuda'

# Phase identifier (for configure_optimizers_grassmann_phases)
grassmann_phase = 'phase3'  # c_attn + mlp.c_fc (non-skip-facing only)

Overriding: init_from = resume
tokens per iteration will be: 393,216
Resuming training from /net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384
tokens per iteration will be: 393,216
Resuming training from /net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384
tokens per iteration will be: 393,216
Resuming training from /net/scratch2/junyuren/nanoGPT-manifold/out-gpt2large-grass-hybridgate-r384

=== Training Complete ===
End time: Fri Jan  2 11:39:22 AM CST 2026
